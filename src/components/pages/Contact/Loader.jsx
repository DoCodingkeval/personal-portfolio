const Loader = () => {
  return (
    <div className="h-max w-max text-5xl text-white">
      <span className="animate-brace inline-block">{`{`}</span>
      <span className="animate-brace inline-block">{`}`}</span>
    </div>
  );
};

export default Loader;
